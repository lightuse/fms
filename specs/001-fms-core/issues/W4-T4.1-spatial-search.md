# Issue: W4 - T4.1 空間検索 API（半径N km 内の利用可能なUnits）

- Estimate: 1.5d
- Labels: backend, db, gis
- Description:
  - PostGIS を使った proximity search API を実装する。結果は距離でソートし、RLS を尊重して返す。
- Dependencies: Units テーブル, PostGIS, RLS
- Acceptance Criteria:
  - 指定 lat/lon + radius_km に対して利用可能な units を返す
  - レスポンスに distance フィールドを含む
  - テナント隔離が機能している
