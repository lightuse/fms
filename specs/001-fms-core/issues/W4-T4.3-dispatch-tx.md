# Issue: W4 - T4.3 指令発行トランザクション（Dispatch）と競合防止

- Estimate: 0.5d
- Labels: backend, db
- Description:
  - 指令発行を DB トランザクションで処理し、同一 Unit への重複指令を防止するロジックを実装する。
- Dependencies: DispatchEvent モデル, DB
- Acceptance Criteria:
  - 同時に2つの指令が来た場合、一方のみがコミットされるかロックで回避される
  - 競合時のエラーはクライアントに明示される
